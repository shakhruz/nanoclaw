## Wiki — Second Brain (Contributor)

Общая вики по адресу `<WIKI_ROOT>` (`/workspace/global/wiki/`, mount read-write). Полная спека — в container skill `wiki` (`/app/container/skills/wiki/SKILL.md`, автоматически в контексте).

**Твоя роль — contributor:**
- ✅ **Читать всё** — `index.md` сначала, потом drill-down в нужные страницы для query
- ✅ **Писать в `inbox.md`** — короткие непереваренные мысли/находки/контекст из своей роли. Формат: `- [YYYY-MM-DD HH:MM] контент`
- ✅ **Создавать страницы в `projects/<твоя-роль>/`** — например `projects/channel-promoter/...`. Только в своей поддиректории.
- ❌ **НЕ модифицируй** `index.md`, `log.md`, `essentials.md`, `tags.md` — это владение curator'а
- ❌ **НЕ трогай** чужие страницы (entities, concepts, people, sources) — если нужна правка, оставь заметку в `inbox.md`

**Curator (main Мила)** видит твои inbox-записи в утреннем брифе и promote'ит в формальные страницы.

**Никогда секреты в wiki** — даже в inbox. Используй onecli vault.
